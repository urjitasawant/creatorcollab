<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CreatorCollab">
        
        <activity
            android:name=".ui.activities.DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="false"
            android:label="All Collaborations"
            android:parentActivityName=".ui.activities.DashboardActivity" />

        <activity
            android:name=".ui.activities.AddEditCollabActivity"
            android:exported="false"
            android:label="Add/Edit Collaboration"
            android:parentActivityName=".ui.activities.DashboardActivity" />

        <activity
            android:name=".ui.activities.CollabDetailActivity"
            android:exported="false"
            android:label="Collaboration Details"
            android:parentActivityName=".ui.activities.DashboardActivity" />

    </application>

</manifest>